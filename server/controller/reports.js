const Report = require('../models/groups');
const db = require('../models/groups');

module.exports = {
  create: {
    post: (req, res) => {

    }
  },
  report: {
    get: (req, res) => {

    }
  }
}
